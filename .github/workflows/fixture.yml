name: Ground Truth P22-E04
on:
  workflow_dispatch:
permissions:
  pull-requests: write
jobs:
  fixture:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Ground-truth target operation
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh api --method PATCH "repos/${{ github.repository }}/pulls/1" -f title=ground-truth
